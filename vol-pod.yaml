apiVersion: v1
kind: Pod
metadata: 
  name: vol-pod
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      volumeMounts:
        - name: html-volume
          mountpath: /usr/share/nginx/html
  volumes:
    - name: html-volume
      emptyDir: {}          